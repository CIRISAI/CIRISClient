# ciris-client-agent

The agent build (node + brain) bundles, as a Python distribution. Consumers do not depend on this
directly — they depend on `ciris-client[agent]`.

See the [repo README](../../README.md) § *The consumption contract*.

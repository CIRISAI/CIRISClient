# ciris-client-node

The AI-free node client bundles, as a Python distribution. Consumers do not depend on this
directly — they depend on `ciris-client[node]`.

See the [repo README](../../README.md) § *The consumption contract*.
